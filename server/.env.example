# Cavendo Engine Configuration
# See the root .env.example for the full list of configuration options.
#
# Copy this file to .env and customize as needed

# Server port (default: 3001)
PORT=3001

# CORS origin for UI (default: http://localhost:5173)
# Set to your UI URL in production
CORS_ORIGIN=http://localhost:5173

# Node environment (development, production)
NODE_ENV=development

# Proxy (set to 'true' or number of proxies when behind nginx/load balancer)
# TRUST_PROXY=true
